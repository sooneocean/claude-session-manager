Screen {
    layout: vertical;
}

Horizontal {
    height: 1fr;
}

#session_list {
    width: 60%;
    height: 100%;
}

#detail_panel {
    width: 40%;
    height: 100%;
    border-left: solid $primary;
}

#session_header {
    height: 1;
    background: $surface;
    color: $text;
    padding: 0 1;
}

#output_log {
    height: 1fr;
}

#status_bar {
    dock: bottom;
    height: 1;
    background: $accent;
    color: $text;
    padding: 0 1;
}
